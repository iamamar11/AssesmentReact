Using shouldupateCompoent method here.

The moment the value of the counter value for the cockpit change the selectComponent value is also changed
which mean render method renders each and every component inside the render method,which leads to slow execution
of the application